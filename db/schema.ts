import { sqliteTable, text, integer, index, uniqueIndex } from "drizzle-orm/sqlite-core";
export const readings=sqliteTable("readings",{id:text("id").primaryKey(),ownerHash:text("owner_hash").notNull(),requestKey:text("request_key").notNull(),topic:text("topic").notNull(),preference:text("preference").notNull(),quoteIds:text("quote_ids").notNull(),generationStatus:text("generation_status").notNull().default("editorial"),modelData:text("model_data"),createdAt:integer("created_at").notNull(),expiresAt:integer("expires_at").notNull()},t=>[index("readings_expiry_idx").on(t.expiresAt),uniqueIndex("readings_owner_request_idx").on(t.ownerHash,t.requestKey)]);
export const reveals=sqliteTable("reveals",{id:text("id").primaryKey(),readingId:text("reading_id").notNull().references(()=>readings.id,{onDelete:"cascade"}),quoteId:text("quote_id").notNull(),createdAt:integer("created_at").notNull()},t=>[uniqueIndex("reveals_reading_quote_idx").on(t.readingId,t.quoteId)]);
export const feedback=sqliteTable("feedback",{id:text("id").primaryKey(),readingId:text("reading_id").notNull().references(()=>readings.id,{onDelete:"cascade"}),quoteId:text("quote_id").notNull(),fit:text("fit").notNull(),reason:text("reason"),createdAt:integer("created_at").notNull()},t=>[uniqueIndex("feedback_reading_quote_idx").on(t.readingId,t.quoteId)]);
export const rateLimits=sqliteTable("rate_limits",{key:text("key").primaryKey(),count:integer("count").notNull(),expiresAt:integer("expires_at").notNull()},t=>[index("rate_expiry_idx").on(t.expiresAt)]);
