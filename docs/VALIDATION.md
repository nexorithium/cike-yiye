# 本轮验证

- TypeScript 类型检查通过。
- 本地 API 集成测试：空白/超长输入、跨域请求拒绝、三枚不重复且不同作品的书签、同书签重复打开一致、跨会话读/揭晓/反馈/删除拒绝、反馈持久化、创建幂等、已知危险表达分流、清除后拒绝读取，均通过。
- 本地合成会话强制过期后读取返回 404 并清理，通过。
- 浏览器完整操作：选择疲惫情境、揭晓、保存原文、不贴合及原因反馈、刷新恢复、收藏列表、再次阅读弹窗、清除阅读，均通过。
- 390px 阅读页与首页、320px 首页无水平溢出；桌面阅读主文字16px。图片加载与桌面/移动截图已检查。
- 浏览器扩展在 html/body 注入属性引发的开发环境 hydration warning，来源为用户浏览器扩展；未通过修改或关闭用户扩展规避。
- 当前不含模型调用，不以以上检查宣称真实用户贴合率或心理有效性。公众试用与人工安全审核尚未进行。
- WebMCP：浏览器实际注册 start_situation_reading，schema/annotations 已核对；无效 topic 被拒绝，有效 topic=read 返回 ready 与三枚书签，页面同步进入选书签。

## 知乎接入验证（2026-09-12，取代上文“当前不含模型”状态）

- TypeScript 与生产构建通过；构建输出 73 个文件扫描无实际密钥。
- model-contract：候选 ID 白名单、重复 ID/格式/截断拒绝、禁止行动约束、AES-GCM 往返、跨 owner/reading 解密拒绝、模型错误不自动重试，通过。
- reading-api：不调用模型的完整会话验证、来源检查、反馈、幂等、跨 owner 访问拒绝、删除，通过。
- 真实服务经本地 Worker：备考半年无反馈、拒绝建议，返回 AI 解读且行动为 null；开店决策返回先验证客流的小步骤。生成分别约 5.0 秒、5.5 秒；幂等创建和刷新返回相同解读，跨 owner 404。
- 三案例 live 脚本首次在第三项遇到供应商 429，未把整套脚本标为通过；随后单独验证迫近危险案例返回 support。不宣称稳定识别全部风险。
- 浏览器纯阅读语义输入真实生成成功；此前一次格式错误时显示可重试错误且保留输入。模型有随机性，存在非 JSON 和限流响应，不以少量样本宣称广泛可靠。
- 浏览器揭晓与刷新恢复逐字一致，来源标识为“知乎直答 · 本次生成”，纯阅读无 action-box；390px 无水平溢出，移动阅读截图检查通过。
