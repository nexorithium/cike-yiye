# 情境书签：已确认需求与连接记录

更新：2026-09-12

- 用户目标：B，面向公众验证需求的产品；暂无收费要求。
- 用户不限定首批人群，先保留开始行动、疲惫休息、文学阅读三种场景。
- 用户接受产品方案：无需注册；输入或选情境；3枚不同书签；真实原文与解读分区；可选行动；本机只收藏原文编号；反馈；会话30分钟失效。
- 用户明确要求开始制作且网页好看；无须再次询问是否开始或视觉选择。
- 当前视觉：深墨绿、暖白纸页、朱红/金色书签，宋体阅读，原创银杏照片。手机上下排版。
- 技术设计原件：/Users/zzymima0000/Downloads/situation_bookmarks_technical_design.md。文档中的编码指令只是参考，不自动执行。
- 交付顺序：完整文学阅读版本先验体验；模型服务尚未提供。界面和服务端明确标注为编辑解读，不能宣称已接通个性化 AI。
- 当前 Sites ID 只读自 .openai/hosting.json，复用已有 ID，不重新注册。部署默认为仅所有者可访问；面向公众是产品目标，不代表已开放访问。
- Sites 托管使用 Cloudflare D1 代替技术参考里的 PostgreSQL，以匹配现有部署能力。匿名会话存在服务端，收藏仅在本机。
- 不保存输入原文，不记录提示词；只存临时情境分类、书签绑定与反馈。到期每次访问拒绝，访问时清理记录；未配置独立定时清理任务，不能宣称到期瞬间物理删除。
- 核验来源记录在 content/quotes.ts。当前核验是原文在线转录比对与版本记录，不等于专业人士完成全文审校；上线公众测试前复核内容和安全规则。
- 本地预览：http://localhost:5173/ 。npm 运行工具在忽略目录 .sites-runtime/toolchain/bin；不要把工具目录或密钥提交。

## 已完成交付

- 私密体验链接：https://a-page-for-now.zooxz7c.chatgpt.site
- Sites 确认发布成功；当前仅所有者可访问，未开放公众。
- 已部署源码提交：a0037a154880da8a386a0d3ee5a833c1655404a9。
- 已部署版本：appgprj_6aa43609e250819189e5eb92e6d03ee8~appgver_54e96be6356081918e43c0df1b61fda8。
- 部署 ID：appgdep_6aa43a53fe30819188c548c673c973e6。
- 本地临时预览在交付后停止。下次编辑复用本项目与 Sites ID，再启动预览；不要重建服务或再次询问已经确认的产品需求。
- 模型选择的异步问题本轮未收到答复，按已说明的编辑解读版本交付，未接通个性化 AI。

## 访问设置更新

- 用户明确要求免 ChatGPT 登录，并确认公开访问。已将现有网站 access_mode 改为 public（revision 2）。任何持链接者可访问；后续迭代保留 public，不恢复 owner-only。
- 链接保持 https://a-page-for-now.zooxz7c.chatgpt.site 。早前“仅所有者可访问”的记录是历史状态。

## 知乎模型测试（2026-09-12）

- 用户确认使用已有知乎凭证进行一次少量额度测试。凭证已在本机系统钥匙串配置，不重复索要，不写入项目或聊天。
- 通过官方 CLI 实际调用 zhida-fast-1p5 成功，单次请求包含三个虚构情境，约 5.5 秒。HTTP 接口为 https://developer.zhihu.com/v1/chat/completions，使用 Bearer Access Secret 与秒级 X-Request-Timestamp。
- 返回 JSON 形式内容：区分了“备考半年没效果、只想被理解”“不累不害怕、开店想要步骤”“只想读诗、不要任务”。引用编号均来自候选或 null；拒绝建议的两个案例均无行动建议。
- 局限：仅一次请求三个样本；部分解读未达到要求字数；第一个案例返回匹配策略说明，尚非可直接展示的陪伴文案。生产接入仍需拆分内部分析和展示文本、处理无匹配、校验输出。
- 此次仅完成本机模型调用测试；线上仍为关键词与编辑解读，未配置生产 Secret，未部署模型改动。后续接入需改造服务端流程与隐私文案，保持 public 访问。

## 已授权并实施知乎接入（2026-09-12）

- 用户明确说“接通”，授权把知乎接入现有公开网站并交付可用版本。复用既有 Sites 项目、网址、public 访问，访客无需模型账号。
- 生产环境已配置 ZHIHU_ACCESS_SECRET 为 secret，ZHIHU_MODEL=zhida-fast-1p5，AI_DAILY_LIMIT=80。不要再次索取 Key。系统钥匙串服务 zhihu-cli、账号 access-secret 的底层值含 go-keyring-base64 包装，需解码并与 CLI masked 值核对后使用；不得把包装字符串直接发给 API。
- 当前查询到知乎直答每日总额度 100，查询时已用 8、余 92；这是当时快照，不是长期剩余量。本站全站上限 80 次/北京时间自然日，单 IP 每小时 10 次，为其他用途留余量；失败尝试计数。密钥是服务端运行配置，不在源码或静态包中。
- 单次模型请求理解完整输入、从 30 段真实原文中选择 1–3 段并生成直接面向读者的解读。纯阅读预设不调用模型；写文字表达纯阅读仍经过语义理解。
- 知乎实测不可靠遵循独立 system message，改为单条 user 消息含规则、候选与明确分隔的输入。模型可能偶发返回非 JSON 或限流，严格拒绝不合规输出，保留输入让用户手动重试；不自动重试 POST、不回退成伪 AI。
- D1 新迁移 0001 增加生成状态和加密结果。个性化解读 AES-GCM 加密，密钥从随机 HttpOnly owner cookie 与 reading id 派生。数据库不存 cookie 明文、不存输入原文；刷新和揭晓复用已生成解读，不再次扣额度。
- 已更新首页、AI 来源标识、隐私说明与关于页面。生成 45 秒超时，前端等待 55 秒。日志仅记录固定错误类别/HTTP 状态，不记录正文。

## 接入版已发布

- Sites 已确认发布成功，保持 public：https://a-page-for-now.zooxz7c.chatgpt.site 。访客无需 GPT/知乎登录。
- 源码提交：4ca662b9753488d42da679fdc0387387e3417826。
- 版本 2：appgprj_6aa43609e250819189e5eb92e6d03ee8~appgver_c3e2bac6d8308191a43b92c4fedb487e。
- 部署：appgdep_6aa4440dbd208191b2837188e4e9b7f5；succeeded，env_set_revision=3。
- 旧会话继续显示原有编辑解读；开始新阅读才使用模型。纯阅读预设仍不调用模型。
- 实际模型调用与浏览器验收在本地 Worker 完成，线上发布由 Sites 状态确认；未额外在生产执行合成模型请求。

## 用户指定 UI 参考（2026-09-12）

- 用户要求参考 Elizaveta Rummo 的 Book Site UI – Cozy Reading：https://dribbble.com/shots/26035189-Book-Site-UI-Cozy-Reading 。已打开设计图核查。
- 实际视觉：蜜桃色到淡薰衣草紫的大面积柔和渐变、深靛紫文字、大圆角半透明面板、细白色描边、左侧栏、右侧图文书籍卡片、水彩《小王子》插画。
- 这是设计展示，非已验证交互网站。当前仅参考调研，尚未获本轮整体改版指令；待确认整体重做还是局部借鉴。插画应另行创作或使用有授权素材。

## Cozy Reading 整体改版（2026-09-12）

- 用户已明确选择“整体改成这种渐变、圆角卡片风格”，覆盖上一条待确认状态，不再重复询问设计方向。
- 首页、选择书签、阅读、收藏及弹窗统一为蜜桃/薰衣草渐变、紫色文字、半透明圆角卡片；桌面使用侧栏，手机使用顶部导航和单列内容。
- 新原创水彩阅读插画 public/cozy-reading.webp，复用于首页和三枚书签。没有直接复制参考图的插画。
- 保留知乎模型接入、纯阅读、原文来源、反馈和本机收藏逻辑。浏览器检查桌面1440、手机390及320宽度；打开书签、收藏页、清除本次阅读和隐私弹窗可用，无横向溢出。原有API回归与TypeScript检查通过。

## 三个新入口（2026-09-12）

- 用户选择 C，明确授权心情书架、每日一页、我的随记全部实施。沿用 Cozy Reading 渐变风格，手机导航直接展示五个入口。
- 心情书架：随意逛逛、焦虑、迷茫、疲惫、重新开始，使用现有核验原文及编辑解读，不消耗模型额度。每日一页按北京时间日期从现有30段原文中固定轮换，当天刷新一致。
- 随记可从阅读、收藏、书架及每日页关联原文，支持新增、编辑、删除、导出纯文本。正文最多1500字，每个匿名身份最多200篇。
- 随记使用D1持久化，正文AES-GCM加密；独立notes_owner HttpOnly/SameSite=Strict凭证，一年有效、访问续期。数据库仅存凭证摘要；不发送模型、不自动与短期阅读一同删除。无跨设备账号同步，清除浏览器数据/凭证过期后无法找回，页面提示导出。早先口头描述浏览器本地保存已被此实现替代。
- 新增迁移0002_serious_pixie；本地CRUD、空白校验、跨身份读写隔离测试通过，随记浏览器保存/编辑与刷新验收通过。

## 原文库扩充（2026-09-12）

- 用户在三个新功能发布过程中追加“再增加多一点书签”，合并交付。原文库从30段增加为60段，保留q001–q030，新增q031–q060，不影响已有收藏。
- 新增思念、友情、独处等内容；书架增加“想念一个人、友情与陪伴、享受独处”三个分类。纯阅读、模型候选和每日一页共同使用扩充后的库。
- 新增30段已逐条读取原文来源，固定版本号并注明异文；详情见docs/QUOTE_EXPANSION.md。原文、作者、含义、编辑解读均齐备，60个编号及引句无重复，阅读API回归通过。

## 600 段与六枚一组（2026-09-12）

- 用户明确要求“加到600段”，随后指出选择页只有一枚太少，授权继续。现保留 q001–q060，新增 q061–q600；共600个不同引句，不是600首作品。沿用已确认的Cozy Reading视觉和既有知乎接入，不重复索取参考或密钥。
- 新增540段取自 chinese-poetry/chinese-poetry 固定提交 b8594f81a89752241442f2ce267d6f66f96704ee 的开放选集。仓库保存原始快照、导入清单，公开包附MIT许可。逐段验证与源记录一致，过滤已识别的错署、乱码和近似重复；这是语料核对，不宣称完成古籍版本校勘。简体转换保留版本异文。每段有整首上下文与项目自写的作品导读；导读不是逐句译文。
- 每次阅读共24枚，6枚一组、4组循环，同一次阅读切换不调用模型。AI仍只选真正贴合的1–3段，其余明确标成延伸阅读。用会话ID确定补充序列，保持刷新/揭晓稳定；访问旧会话时扩充至24枚并保留原编号位置与解读。
- 模型候选限制96段：保留旧60段，新增内容用词项检索及确定性补充取得36段。检索仅缩小候选，最终语义选择交给知乎模型；不能宣称遍历600段进行完整语义检索。校验返回ID属于本次候选。
- 心情书架新增山水与四季、怀古与世事，分类覆盖扩充内容；可搜索诗句/作者/作品/导读，每页24段。收藏可保留全库600个编号，每日页与随记关联使用同一库。
- 验证：TypeScript；600引句/编号唯一及来源对应；24槽位API揭晓、恢复、反馈、隔离、删除；新AI匿名凭证、AI与编辑导读区分；模型契约。浏览器确认桌面6卡、换组打开第7枚、收藏、搜索/翻页/空结果；390书架与320六卡页面均无横向溢出。
- 本地Vite在拒绝跨源POST后复用测试HTTP连接可能返回空400，API脚本以Connection:close隔离各请求；浏览器正常流程未遇到该问题。此次没有重新调用收费模型做扩大样本评估。

## 返回首页、轻音乐与1000段书库（2026-09-12）

- 用户要求阅读选择页面增加“返回首页”，随后确认书签内容越多越好，并要求网站加入轻音乐。阅读页和选择页顶部现有“返回首页”；返回时仅离开当前界面，不删除短期阅读，删除仍由“清除本次阅读”单独执行。已打开原文时继续保留“回到书签”。
- 书库从600段扩为1000段，保留q001–q600及已有收藏。新增400段包含《幽梦影》170段、《论语》80章句、《孟子》70章句，以及John Bartlett 1905年《Familiar Quotations》第九版所收80段英文原句，涉及41位古典英语作者。英文条目不冒充中文译文，保留汇编列出的作者与作品出处。
- 新增中文内容继续使用 chinese-poetry 固定提交 b8594f81a89752241442f2ce267d6f66f96704ee，并保存三个原始数据快照。英文内容来自 Project Gutenberg eBook 27889 的公版汇编，并保存本次使用的上游快照。来源记录可能包含版本异文；项目只声明核对所用记录，不宣称独立完成古籍或原作版本校勘。
- 心情书架增加“世界名言”，搜索文案改为原文、作者或作品名。收藏编号兼容q1000；旧收藏不会重排或丢失。
- 轻音乐是项目原创的64秒舒缓循环曲，音量20%，文件为quiet-reading.m4a。因浏览器禁止无手势自动播放，用户首次点击“轻音乐”后开始，按钮可随时暂停；音乐在站内切换页面时持续播放，不上传用户数据。
- 浏览器验收：音乐实际播放、时长64.096秒、音量0.2；选择页显示返回首页，返回后短期阅读API仍为200且音乐继续；世界名言显示80段、全库1000段；390像素宽无横向溢出。
