import {body,db,guard,guidance,json,owned,selected} from "@/lib/server";
export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){return guard(async()=>{const input=await body(req);const {id}=await params;const row=await owned(req,id);const q=selected(row,input.bookmark_id);await db().prepare("INSERT OR IGNORE INTO reveals(id,reading_id,quote_id,created_at) VALUES(?,?,?,?)").bind(crypto.randomUUID(),id,q.id,Date.now()).run();return json({slot:input.bookmark_id,quote:q,guidance:await guidance(req,row,q)})})}
